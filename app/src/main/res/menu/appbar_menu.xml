<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter"
        android:color="@color/white"
        android:tint="@color/white"
        android:title="Search"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/menuAnimeFilterAll"
                android:title="All" />
            <item
                android:id="@+id/menuAnimeFilterViewed"
                android:title="Viewed" />
            <item
                android:id="@+id/menuMangaFilterRead"
                android:title="Read" />
            <item
                android:id="@+id/menuGamesFilterFinished"
                android:title="Finished" />
            <item
                android:id="@+id/menuGamesFilterTried"
                android:title="Tried" />
            <item
                android:id="@+id/menuAnimeFilterInProcess"
                android:title="In Process" />
            <item
                android:id="@+id/menuAnimeFilterDropped"
                android:title="Dropped" />
        </menu>
    </item>


    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:color="@color/white"
        android:tint="@color/white"
        android:title="Search"
        app:showAsAction="ifRoom" />
</menu>